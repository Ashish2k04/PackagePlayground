# Port on which the Express server will run
PORT=3000

# Application environment used by the error middleware
# In development mode, the error stack is included in the response
NODE_ENVIRONMENT=development