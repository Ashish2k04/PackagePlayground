info:
  name: Create-note-api
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/create-note
  body:
    type: json
    data: |-
      {
        "title": "test_title",
        "description": "test_description"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
